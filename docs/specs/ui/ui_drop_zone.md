# UI Drop Zone Specification

## File
`core/include/ui/ui_drop_zone.h`

## Purpose
Specializes a `UI_Element` as a drop zone — a region that can receive
draggable elements when they are dropped onto it.

## Dependencies
| Header | Purpose |
|--------|---------|
| `defines.h` | `UI_Element`, `m_UI_Receive_Drop` |
| `defines_weak.h` | Forward declarations |

## Functions

### `initialize_ui_element_as__drop_zone`
