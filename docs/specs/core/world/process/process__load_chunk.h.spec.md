# 1 Specification: core/include/world/process/process__load_chunk.h

## 1.1 Overview

Header for chunk loading process functionality. Currently contains no
function declarations — the load chunk process handler is likely declared
elsewhere or pending implementation.

## 1.2 Dependencies

- `defines.h` (for `Process`, `Game`)

## 1.3 Functions

None declared.

## 1.4 Agentic Workflow

### 1.4.1 Status

This header is a placeholder. Chunk deserialization is handled by
`m_process__deserialize_chunk` declared in `chunk.h`.

## 1.5 Header Guard

`PROCESS__LOAD_CHUNK_H`
