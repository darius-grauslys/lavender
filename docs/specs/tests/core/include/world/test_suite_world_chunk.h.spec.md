# Specification: tests/core/include/world/test_suite_world_chunk.h

## Overview

Generated header for the `chunk.c` module suite (in `core/source/world/`).
Preserved across regeneration.

## Dependencies

- `<test_util.h>`

## Declaration

    DECLARE_SUITE(chunk)

## Usage

Included by `world/MAIN_TEST_SUITE__CORE_WORLD.h`. Registered via
`INCLUDE_SUITE(chunk)`.
