# Specification: tests/core/source/MAIN_TEST_SUITE__CORE_ANCIENTS_GAME.c

## Overview

Generated root test suite source. Wires all top-level module suites and
directory sub-suites into the root `MunitSuite`. Always regenerated by
`update_recursive.sh`.

## Dependencies

- `MAIN_TEST_SUITE__CORE_ANCIENTS_GAME.h`
- `test_util.h` (for `INCLUDE_SUB_SUITES`, `DEFINE_SUITE_WITH__SUB_SUITES`,
  `INCLUDE_SUITE`, `END_TESTS`)

## Generated Content

    INCLUDE_SUB_SUITES(CORE_ANCIENTS_GAME, 26,
        INCLUDE_SUITE(main),
        INCLUDE_SUITE(vectors),
        INCLUDE_SUITE(core_string),
        INCLUDE_SUITE(random),
        INCLUDE_SUITE(client),
        INCLUDE_SUITE(degree),
        INCLUDE_SUITE(game),
        INCLUDE_SUITE(CORE_INPUT),
        INCLUDE_SUITE(CORE_RENDERING),
        INCLUDE_SUITE(CORE_WORLD),
        INCLUDE_SUITE(CORE_INVENTORY),
        INCLUDE_SUITE(CORE_SORT),
        INCLUDE_SUITE(CORE_DEBUG),
        INCLUDE_SUITE(CORE_AUDIO),
        INCLUDE_SUITE(CORE_LOG),
        INCLUDE_SUITE(CORE_SERIALIZATION),
        INCLUDE_SUITE(CORE_GAME_ACTION),
        INCLUDE_SUITE(CORE_ENTITY),
        INCLUDE_SUITE(CORE_UI),
        INCLUDE_SUITE(CORE_COLLISIONS),
        INCLUDE_SUITE(CORE_UTIL),
        INCLUDE_SUITE(CORE_MULTIPLAYER),
        INCLUDE_SUITE(CORE_RAYCAST),
        INCLUDE_SUITE(CORE_PROCESS),
        INCLUDE_SUITE(CORE_SCENE),
        NULL);

    DEFINE_SUITE_WITH__SUB_SUITES(CORE_ANCIENTS_GAME, END_TESTS);

Count 26 = 7 modules + 18 directories + 1 NULL.
